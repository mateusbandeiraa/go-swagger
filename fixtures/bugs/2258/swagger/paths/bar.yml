get:
  responses:
    200:
      description: OK
      schema:
        type: array
        items:
          $ref: '../user/index.yml#/User' ## this doesn't work
    500:
      description: OK
      schema:
        type: array
        items:
          $ref: './swagger/user/index.yml#/User' ## this works